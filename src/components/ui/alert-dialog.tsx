"use client";
import * as React from "react";

export interface Alert-dialogProps extends React.HTMLAttributes<HTMLDivElement> {}

export function Alert-dialog(props: Alert-dialogProps) {
  return <div {...props} />;
}
