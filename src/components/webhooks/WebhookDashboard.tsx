"use client";

import React from "react";

export default function WebhookDashboard() {
  return (
    <div style={{ padding: "40px", textAlign: "center" }}>
      <h1>Webhook Dashboard</h1>
      <p>
        Manage and monitor your connected webhooks here. 
        View recent delivery logs, configure new webhook endpoints, 
        and track API events across your integrations.
      </p>
    </div>
  );
}
